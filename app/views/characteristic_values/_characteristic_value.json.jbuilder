json.extract! characteristic_value, :id, :part_of_characteristic_id, :value, :person_id, :vdate, :vtime, :created_at, :updated_at
json.url characteristic_value_url(characteristic_value, format: :json)
